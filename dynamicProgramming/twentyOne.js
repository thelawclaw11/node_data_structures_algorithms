function costToMultiply(left, right) {
    return left[0] * right[1];
}

function parenthesization(arrayOfDimensions) {
    const dp = Array(arrayOfDimensions.length);
}
console.log(
    parenthesization([
        [1, 3],
        [3, 1],
        [1, 3],
    ])
);
